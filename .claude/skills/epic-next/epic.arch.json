{
  "name": "Reusable shared calendar library (libs/shared/calendar)",
  "epicKey": "ARCH-60",
  "jql": "parent = ARCH-60 AND statusCategory = \"To Do\"",
  "storyOrdering": "epic-description",
  "groupLabel": "Wave",
  "completionMessage": "All ARCH-60 stories are complete!",
  "phaseNote": null,
  "hybridMode": false,
  "commitScope": "calendar",
  "buildCommand": "npx nx run shared-calendar:test",
  "app": null,
  "libraries": {
    "ui": null,
    "store": null,
    "designLibrary": "libs/shared/calendar"
  },
  "routingFiles": [],
  "conventions": [
    "Signals: input() / output() — NEVER @Input() / @Output()",
    "DI: inject() — NEVER constructor injection",
    "Control flow: @if, @for, @switch — NEVER *ngIf / *ngFor",
    "Separate files: .ts, .html, .scss, .spec.ts — NEVER inline template/styles",
    "Testing: Jest only — NEVER Jasmine",
    "Tailwind utility classes first; SCSS with BEM only for complex layouts/animations",
    "Shared component lib libs/shared/calendar: ViewEncapsulation.None + host: { class: 'giga-...' }; SCSS wrapped in .giga-calendar (never :host); a COMPLETE self-sufficient --giga-calendar-* default token set with literal fallbacks — var(--giga-calendar-x, <literal>) — so it renders with no host theme. NO --mat-sys-* / Angular Material fallback layer (Material is being removed from the workspace, decided 2026-06-07); consumers theme EXCLUSIVELY via --giga-calendar-* overrides (ancestor class or the `theme` input). Centralized in theme/_giga-calendar-tokens.scss ($tokens map + giga-calendar-token() helper).",
    "Multi-component state: a component-scoped @ngrx/signals GigaCalendarStore provided at the root <giga-calendar> (providers:, NEVER providedIn root); child views/cells/chips inject() it; input()/output() ONLY at the root public boundary — see docs/ai-instructions/multi-component-signal-store.instructions.md + docs/ai/CONSTRAINTS.md",
    "Pure layout + date logic stays in pure functions (libs/shared/calendar core + libs/shared/date); the store calls them from withComputed",
    "NOT D3 — HTML + CSS Grid + @angular/cdk/drag-drop (DOM-native DnD)",
    "Stories live INSIDE the lib and are composed by apps/storybook-app (add the glob to apps/storybook-app/.storybook/main.ts stories array); serve via npm run storybook (nx storybook storybook-app); shared GigaStorybookReviewContainerComponent from libs/shared/storybook",
    "Date math: libs/shared/date (date-fns-backed); reuse NgPatTimeStamp from @gigasoftware/api for input normalization"
  ],
  "designReferences": null,
  "epicEvolutionRules": [
    "Capture architectural shifts in THIS epic. When a story surfaces a constraint, pivot, or gap, update the subsequent stories that depend on the decision; do not silently absorb the change in the current story, and do not split the work into a separate epic.",
    "Update downstream discovery / research stories with upstream decisions before they are picked up — discovery work should not re-derive choices that have already been settled in earlier stories of this epic.",
    "Add newly-discovered work as CHILD tickets of THIS epic — never as prose in the epic description. Create them 'To Do' if ready to be worked next (`/epic-next`'s JQL only picks up 'To Do' stories), or 'Backlog' if deferred / not-yet-ready. Reference each in the wave plan as a one-line `KEY — summary` entry.",
    "The epic description holds ONLY: the overall description, the wave order (one line per story, `KEY — summary`), and — at most — a few high-level pivot / architectural-lock bullets. NEVER put per-story implementation detail, acceptance criteria, locked-invariant prose, or follow-up specs in the epic; that detail lives in the child ticket whose scope it is (or the linked design docs). Scope must be readable from the child ticket alone. Keep the wave plan and the actual story set in sync as scope evolves — never let them drift, and never let an entry grow past one line."
  ]
}
