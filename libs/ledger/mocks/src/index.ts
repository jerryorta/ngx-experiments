// @nge/ledger-mocks — seed data (accounts, categories, ~3-6 months of
// transactions, budgets, bills) served as the mock backend.
export {
  ledgerAccounts,
  ledgerBills,
  ledgerBudgets,
  ledgerCategories,
  ledgerSeed,
  ledgerTransactions,
  type Account,
  type Bill,
  type Budget,
  type Category,
  type Transaction,
} from './lib/seed';
