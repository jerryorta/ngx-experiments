// @nge/ledger-themes — scss design-token library. Defines --ldg-* semantic aliases
// over the shared --dlc-* persona tokens (+ ledger-only money / category tokens).
// Consumed via scss @use of ./lib/styles/ldg-themes.scss; no TS runtime exports.
export {};
